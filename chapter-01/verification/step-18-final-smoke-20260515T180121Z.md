# Chapter 1 / Step 18 final smoke report

**Generated:** 2026-05-15T18:01:21Z.
**Mode:** synthetic end-to-end against production (signed-webhook simulation of Stripe upgrade; no real card).
**Test user:** `88f0c701-1c71-4f2c-98f8-84dc0a04309c` · `nizzar.ben+c1close-20260515T171614@gmail.com`. Fresh row, plus-aliased to a real inbox for any later manual confirmation.

## Result summary

| Stage | Outcome | Notes |
| --- | --- | --- |
| Fresh user create | ✓ | admin/users POST + handle_new_user trigger |
| Phase 01 seed (Archetype + Soul Map + Sensescape) | ✓ | PostgREST PATCH of qbp + tool_completions |
| Session mint | ✓ | admin generate_link → action_link → access_token |
| `POST /api/lock-foundation` | **504 in 25.5 s** | Known regression from PR #63 revert. **Lock + dispatch succeeded server-side**; all 4 artifacts delivered. The 504 is a UX bug, not a data bug. |
| Artifacts delivered | 4 of 4 (`delivered`) | Soul Map, Sensescape, Visual DNA, War Table |
| Emails delivered (Resend) | 5 of 5 (`delivered`) | Foundation locked + 4 artifact-ready, all confirmed via Resend events |
| Free-tier surfaces | ✓ | `/api/qbp` tier=free, `/api/artifacts` 4 rows / 3 locked, `/api/qbp/export` 402 export_gated |
| Stripe upgrade (signed webhook) | ✓ | `checkout.session.completed` with `tier_intent=starter` → tier flipped to starter in <2 s |
| Post-upgrade surfaces (tier-server-authoritative) | ✓ | `/api/qbp` tier=starter; `/api/artifacts` 4 rows / 0 locked; `/api/qbp/export` 200 with signed_url |
| Cleanup | ✓ | Tier reset to free, subscription_status canceled |

## What's verified

1. **End-to-end free → starter path works.** Every API endpoint along the user path behaves correctly. Lock, dispatch, all 4 agent artifacts, all 5 transactional emails, tier-gating both directions.
2. **PR #60 tier-server-authoritative fix works.** Post-upgrade `/api/qbp` returns `tier=starter` and the surfaces that read from it (foundation, archive, qbp, account, artifact) all see the live tier. No more reading from stale `session.tier`.
3. **PR #61 email header change is deployed.** All 5 transactional emails delivered through the new code path (no `List-Unsubscribe` headers on the four transactional templates). Primary-tab placement requires manual Gmail inspection per `EMAIL_DELIVERABILITY.md`; the code-side change is verified.
4. **PR #63 revert is in effect.** Lock endpoint synchronously awaits the 4 agent dispatches as before PR #59. Under contention this returns 504; under the test conditions here, it did return 504. Documented known limitation.

## What's NOT verified in this run

- Real Stripe Checkout submission (used signed-webhook simulation; step 15 already proved the real-card path).
- Visual rendering on the four post-fix surfaces against mobile + desktop viewports. Re-screenshotting via Playwright would require the deployed preview to be reachable, which Vercel auth-gates. The fix is verified API-side and committed code-side; visual confirmation belongs in a follow-up manual pass.
- Gmail Primary-tab placement (still gated on manual run per `EMAIL_DELIVERABILITY.md`).

## Known limitations carried into Chapter 2

1. **Lock endpoint 504 on contention.** Per PR #63 revert decision. Real fix path: durable job runner OR pre-insert artifact rows + reliable background fetch. Migration 010 (`dispatch_jobs` table) stays in the repo and on prod for the eventual fix.
2. **`?upgrade=success` browser flow.** Even though the API tier flips and the surfaces read it correctly, the foundation page's auth gate still uses localStorage `qb_session` for the initial check. If the localStorage session is stale at the moment of redirect, the bounce-to-signal-scan still fires. The tier-display path is fixed (PR #60); the auth-gate path is not. Carry into Chapter 10.
3. **Gmail Primary-tab verification deferred.** Headers are correct per spec; verification log row stays pending.

## Final state of test user

```
id:                  88f0c701-1c71-4f2c-98f8-84dc0a04309c
email:               nizzar.ben+c1close-20260515T171614@gmail.com
tier:                free (reset)
subscription_status: canceled
artifact count:      4 (all status=delivered)
foundation_locked_at: set
```

Preserved in-place for any post-close manual inspection.
