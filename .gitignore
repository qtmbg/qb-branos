# Claude Code local state — skill symlinks point to user's local filesystem
.claude/
skills-lock.json

# Goose tool local state
.goose/

# OS / editor noise
.DS_Store
.idea/
.vscode/
*.swp
.vercel
.env
.env.*
.env*.local
# Production secrets pulled via `vercel env pull` for live harness runs.
# Covered by .env.* above; named explicitly so it can never be committed.
.env.qb-branos.live

# iOS build artifacts
ios/build/
ios/DerivedData/
