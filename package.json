{
  "name": "qb-branos",
  "version": "1.0.0",
  "private": true,
  "description": "QB BrandOS — static site + Vercel Edge Functions. Build step is null; this manifest exists only so Vercel installs the runtime deps below.",
  "license": "UNLICENSED",
  "dependencies": {
    "@vercel/og": "^0.6.4"
  }
}
