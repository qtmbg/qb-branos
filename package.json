{
  "name": "qb-branos",
  "version": "1.0.0",
  "private": true,
  "description": "QB BrandOS — static site + Vercel Functions (Edge + Node serverless). Build step is null; this manifest exists so Vercel installs the runtime deps below. type:module makes the Node runtime treat the ESM api/ files as ESM natively (chapter-3 step 5 runtime migration).",
  "type": "module",
  "license": "UNLICENSED",
  "dependencies": {
    "@vercel/functions": "^2.2.13",
    "@vercel/og": "^0.6.4"
  }
}
